mixin category(name)
	a.category(href=`#${name}` onclick=`nav.handleClick("${name}", this)` data-category=name)
		+icon(name).medium
		label= name

#nav
	.categories
		+category("random")
		+category("body")
		+category("mind")
		+category("performance")
		+category("imagination")
		+category("sight")